query GetUser($id: ID!) {
  user(id: $id) {
    id
    name
    email
    role
  }
}

query ListUsers($limit: Int, $offset: Int) {
  users(limit: $limit, offset: $offset) {
    id
    name
    role
  }
}
